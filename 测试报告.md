# 多端测试报告

日期：2026-08-11  
测试方式：Playwright + Edge（Chromium）无头浏览器，访问本地 HTTP 服务  
测试页面：[index.html](index.html)

## 1. 适配测试（三端视口）

| 设备 | 视口 | 结果 |
| --- | --- | --- |
| 手机 | 390×844 | 通过：无横向溢出，底部 Tab 导航 |
| iPad mini | 768×1024 | 通过：无横向溢出，顶部导航+双栏 |
| PC | 1440×900 | 通过：无横向溢出，三栏布局 |

截图：`测试截图/phone-390-home.png`、`测试截图/ipad-768-home.png`、`测试截图/pc-1440-home.png`

## 2. MVP 验收用例

| 用例 | 内容 | 结果 |
| --- | --- | --- |
| 用例1 | 华北、CKD3-5期、血钾/血磷偏高 → 可用菜谱≥60道（原34），不含高钾/高磷，低钾+低磷优先，无控制台错误 | 通过 |
| 用例2 | 选择土豆/茄子/鸡蛋/西红柿 → 返回可做菜谱并显示缺料提示 | 通过 |
| 用例2b | 替代食材匹配：家里有西葫芦 → 命中“西红柿可用西葫芦替换”等菜谱 | 通过 |
| 分组 | 列表默认按“可直接做 > 差1–2个食材 > 需备齐更多食材”展示 | 通过 |
| 用例3 | 菜谱详情：食材清单、分步做法、营养估算10项（含“占每日参考”比例与“一餐判定”标签）、适用分期、收藏按钮 | 通过 |
| 用例4 | 收藏后出现在“我的收藏”，取消后消失 | 通过 |
| 用例5 | 提醒：3个时间点+每周重复保存；导出 .ics 含 3 个事件与 RRULE | 通过 |
| 用例6 | 170cm/60kg/男/轻体力/CKD3-5期 → 目标卡片正确；智能配餐四餐齐备；累计营养8行含百分比与判定标签；手动换菜后实时更新 | 通过 |
| 用例7 | 累计营养超标时红色高亮、标签“偏高”、绿条变红条（钾行验证，钠/磷/能量等同理） | 通过 |
| 补充 | CKD1-2 血钾正常显示“不限制”；透析期水显示“遵医嘱” | 通过 |
| 补充 | 刷新后档案与授权保留，不再重复弹免责声明 | 通过 |
| 数据 | 用户 37 道菜谱合并去重：134→133 道（清除历史同名重复），新增 34 道营养与食材完整、分期正确 | 通过 |

结果：**53 项断言全部通过（53 PASS / 0 FAIL）**。

## 3. 测试中发现并修复的缺陷

1. 收藏页渲染崩溃：`renderFavs` 生成的卡片缺少 `missing` 字段导致脚本报错、收藏列表空白。已修复并加防御。
2. 收藏按钮事件被“查看做法”抢先：点击列表卡片上的“收藏”会打开详情而非收藏。已调整事件处理顺序。

## 4. 推荐策略优化（2026-08-12）

- 推荐从“硬过滤（必须低钾+低磷）”改为“硬排除慎用菜谱 + 标签优先排序”：血钾/血磷偏高只排除“慎用：高钾/高磷”，其余按低钾/低磷优先展示，中钾/中磷菜谱标注“先焯水、控份量”。双高患者可用菜谱由 34 道提升到 64 道。
- 启用“替代食材”匹配：利用菜谱自带 `substitutes` 字段，家里有 X 时，“缺：Y（可用你家的 X 替代）”计入匹配。
- 列表默认分组：可直接做 > 差1–2个食材 > 需备齐更多食材。

## 5. 营养占比与一餐判定（2026-08-12）

- 菜谱详情“营养估算（参考）”表新增两列：**占每日参考**（按用户档案分期与指标计算每日目标后求百分比）和**一餐判定**（偏低/适中/偏高）。
- 判定标准：按“一餐≈每日参考量 1/3”估算，<15% 偏低、15–33% 适中、>33% 偏高；脂肪/碳水按供能比估算；未填写档案时按成人一般参考值并提示。
- 透析期水、CKD1-2 期钾（不限制）显示“—/遵医嘱”。

## 6. 计划页累计营养占比与判定（2026-08-12）

- “今日累计营养对照”每行新增百分比（如 `248 / 1800 kcal · 14%`）和判定标签（偏低/适中/偏高）。
- 判定按“累计 vs 每日目标”：<60% 偏低、60–100% 适中、>100% 偏高；偏高时绿条变红条、整行红色高亮。

## 7. 用户菜谱合并（2026-08-13）

- 附件 `菜谱整理模板_4234.xlsx` 解析 37 道菜谱，去重后新增 34 道，总库 133 道。
- 去重规则：菜名/基名重复跳过（番茄炒蛋×2、小米粥×2、清蒸鲈鱼、地三鲜）；健康变体（低磷版/低盐版等）保留。
- 新增约 30 种食材进食材库；飞水/去盐食材按指南附录1口径折算钾磷钠。

## 4. 测试截图

`测试截图/use1-recommend-phone.png`、`测试截图/use2-ingredients-ipad.png`、`测试截图/use3-detail-pc.png`、`测试截图/use5-remind-ipad.png`、`测试截图/use6-plan-pc.png`、`测试截图/use7-exceed-red.png`

## 6. 线上部署

- 仓库：https://github.com/Daisy199501/kidney-diet-helper
- GitHub Pages：https://daisy199501.github.io/kidney-diet-helper/（构建状态 built；大陆网络访问 github.io 可能需代理，已提供部署包供国内托管平台上架）
- 部署包：`部署包/肾友食养助手-静态站.zip`

## 5. 说明

- 测试脚本：`scripts/test-site.cjs`（可重复运行）。
- 页面为纯静态，离线可用；数据仅存本机浏览器。
